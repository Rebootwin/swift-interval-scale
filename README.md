# Description

Usage:

```Swift
let fromInterval = -1.0 ... +1.0
let toInterval = -100.0 ... +100.0

fromInterval.scale(-1, toInterval: toInterval) // -100
fromInterval.scale(+1, toInterval: toInterval) // +100
```
